import * as flsFunctions from "./modules/functions.js";

flsFunctions.isWebp();

// Базовая версия + модули навигации, разбивки на страницы:
import Swiper, { Navigation, Pagination } from 'swiper';

const swiper = new Swiper();